import type { RouteRecordRaw } from 'vue-router'

// Routes are intentionally empty at this stage.
// Page slices (pages/*) will register their routes here in a later milestone.
export const routes: RouteRecordRaw[] = []
